#include <stdio.h>
#include "a2blocked.h"
#include "a2methods.h"
#include "a2plain.h"

/*
 * The two functions below are functions you should implement.
 * They should take their input from the parameter and should
 *  write their output to stdout
 */

extern A2Methods_UArray2 compress40  (FILE *input);  /* reads PPM, writes compressed image */
extern void decompress40(A2Methods_UArray2 compressed);  /* reads compressed image, writes PPM */
