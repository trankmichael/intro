#include "dictionary.h"
#include <cstdlib>

Dictionary::Dictionary()
{
    root = new Node;
    root->marker = false;
    emptyAlph(root);
}

bool Dictionary::

void Dictionary::emptyAlph( Node s )
{
    for( int i = 0; i < ALPH; i++ ){
	s->child[i] = NULL;
    }
}
